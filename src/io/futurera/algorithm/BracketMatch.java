package io.futurera.algorithm;

/**
 * Given a string, check if the brackets contained in the string is valid.
 * Return 0 if not valid, or 1 otherwise. A string is considered valid if: 1) no
 * bracket at all; 2) Any open brackets "(", "{", "[" is closed by the same type
 * of brackets; 3) Open brackets are all closed in the correct order. For
 * example: ([)] is false; [][] or [[]] are both true.
 */
public class BracketMatch {
    public static int run(String bracketString) {
        return -1;
    }
}